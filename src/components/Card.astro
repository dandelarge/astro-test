---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<div class="flex rounded-md border-solid border-slate-500 bg-slate-800 p-6 flex-col gap-2 border-2 text-slate-200 w-full">

		<h2 class="text-slate-50 text-lg mb-4 font-mono ">{title}</h2>
		<slot />
</div>
